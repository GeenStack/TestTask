'''
	If the API addresses change, 
	it will be enough to edit the config file 
	instead of editing the addresses in the function code.
'''

users_url = 'https://json.medrating.org/users'
tasks_url = 'https://json.medrating.org/todos'